 <small>
  <td class="center"><%= object.id %></td>
  <td class="center"><small><%= object.name %></small></td>
  <td class="center"><small><%= object.cmeth.nil? ? "Unknown" :  object.cmeth.name %></small></td>
  <td class="center"><small><%= object.inserts.map{|p| "#{p.number} #{p.name}"}[0] %></small><br /></td>
  <td class="center"><small><%= object.backbones.map{|p| "#{p.number} #{p.name}"}[0] %></small><br /></td>
 </small>
  <td class="actions center">
	  <%= smart_listing_item_actions [
	  { name:   :edit, 
  		url:  edit_from_inventory_clone_path(object),
  		},
		{name: :destroy, url: clone_path(object),
			title: "Destroy",
			confirmation: "Are you sure you want to delete this clone?"}]%>
</td>