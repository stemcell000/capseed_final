<%= smart_listing_item :clone_batches, :update, @clone_batch, @clone_batch.valid? ? "clone_batches/smart_listing/clone_batch" : "clone_batches/clone_batch_components/form_pb_from_inventory" %>

//Popover
 $('[data-toggle="popover"]').popover({html:true});