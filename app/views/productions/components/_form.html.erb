<div class="panel-body">
		<%= simple_form_for @production, html: { class: 'form-vertical' }  do |f| %>
			<%= f.error_notification %>
			        <%= f.input :comment %>
					<%= f.association :projects, :label => "Projects",
					:collection => Project.all.order(:name),
					:selected => 1,
					:input_html => { class:"select2-select" } %>
					
					
	 				<div class="btn-group" role="group" aria-label="...">
	      		     	<%= link_to "Cancel", @productions, class: "btn btn-default", data: {:confirm => "All the changes will be lost."}, :title => "Cancel the changes and return to the Home page." %>
	  		        	<%= f.submit("Next", class: "btn btn-default", title: "Save the changes to this Assay.") %>
	  		        </div>
	  	
			<% end %>
			</div>
	</div>