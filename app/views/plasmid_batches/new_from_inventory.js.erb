//Les donnees dans les champs du formulaire form_clone sont testées d'après les critères de validation
//definis dans le model. Si ces criteres sont verifies, les donnees sont sauvees. Sinon, le formulaire
//dans sa fenetre modale reste ouvert et les message d'erreur s'affichent en haut du formulaire.
	$("#myPblabel").html("Plasmid batch - select for production")
	$("#myPbButtons").html("<%= escape_javascript( render'components/modal_close_btn') %>")
	$("#flash-msg-container").html("")
	$("#inner-pb-modal").html("<%= escape_javascript( render'plasmid_batches/plasmid_batch_components/new_from_inventory' ) %>")
	$("#pb-modal").modal("show")
	$("ul.errors").html("")
	console.debug("inventory/new is loaded => Add plasmid_batch");

