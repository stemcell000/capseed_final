= form_for @format do |f|
  - if @format.errors.any?
    #error_explanation
      %h2= "#{pluralize(@format.errors.count, "error")} prohibited this format from being saved:"
      %ul
        - @format.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
